apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  labels:
    app: rabbitmq
spec:
  ports:
  - port: 5672
    targetPort: 5672
    name: basic
  - port: 15672
    targetPort: 15672
    name: management
  type: LoadBalancer
  selector:
    app: rabbitmq
